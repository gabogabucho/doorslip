[project]
name = "doorslip"
version = "0.1.0"
description = "Buzones firmados para agentes personales"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=42.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/doorslip"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
